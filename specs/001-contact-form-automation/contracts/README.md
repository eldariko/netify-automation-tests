# Contracts

This feature is a test automation artifact and does not expose external runtime contracts (APIs/CLI) to other systems. No external interface contracts are required for the contact-form automation test.

If in the future the feature exposes a public API or CLI for other teams, add a contract document here describing the interface, schema, examples, and validation rules.
